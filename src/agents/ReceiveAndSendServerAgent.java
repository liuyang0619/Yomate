package agents;

import jade.core.Agent;

public class ReceiveAndSendServerAgent extends Agent{
	/*
	 * The main agent who receives the request from server
	 * and transfers the requests to the others agents
	 * */
	protected void setup(){
		
	}
}
