package agents;

import jade.core.Agent;

public class SqlExecuteAgent  extends Agent{
	/*
	 * The agent who receives the SQL request from others agents
	 * and response with results
	 * */
	protected void setup(){
		
	}
}
